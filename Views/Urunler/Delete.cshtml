@model HediyelikEsya.Models.Urun
@{ ViewData["Title"] = "Sil"; }

<div class="container mt-5 text-center">
    <div class="alert alert-danger shadow">
        <h3>🗑️ Silmek İstediğine Emin misin?</h3>
        <p class="lead"><strong>@Model.Ad</strong> isimli ürün kalıcı olarak silinecek!</p>
    </div>

    <div class="card w-50 mx-auto shadow-sm">
        <div class="card-body">
            <dl class="row text-start">
                <dt class="col-sm-4">Fiyat:</dt>
                <dd class="col-sm-8">@Model.Fiyat ₺</dd>

                <dt class="col-sm-4">Açıklama:</dt>
                <dd class="col-sm-8">@Model.Aciklama</dd>
            </dl>
            
            <form asp-action="Delete">
                <input type="hidden" asp-for="Id" />
                <button type="submit" class="btn btn-danger btn-lg w-100 mb-2">Evet, Sil</button>
                <a asp-action="Index" class="btn btn-outline-secondary w-100">Vazgeç</a>
            </form>
        </div>
    </div>
</div>