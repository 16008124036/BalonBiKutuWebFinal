@model HediyelikEsya.Models.Urun

@{
    ViewData["Title"] = Model.Ad + " Detayları";
}

<div class="container mt-5">
    <div class="card shadow-lg border-0 rounded-3 overflow-hidden">
        <div class="row g-0">
            <div class="col-md-6 bg-light d-flex align-items-center justify-content-center p-3">
                <img src="~/img/@(string.IsNullOrEmpty(Model.ResimUrl) ? "logo.jpg.jpeg" : Model.ResimUrl)" 
                     class="img-fluid rounded shadow-sm" 
                     style="max-height: 500px; object-fit: contain;" 
                     alt="@Model.Ad">
            </div>
            
            <div class="col-md-6">
                <div class="card-body p-4 p-md-5 d-flex flex-column h-100">
                    <div class="d-flex justify-content-between align-items-start mb-3">
                        <h1 class="card-title display-6 fw-bold text-primary" style="font-family: 'Brush Script MT', cursive;">
                            @Model.Ad
                        </h1>
                        <span class="badge bg-danger fs-4 shadow-sm px-3 py-2 rounded-pill">@Model.Fiyat ₺</span>
                    </div>

                    <h5 class="text-muted mb-3">Ürün Açıklaması:</h5>
                    <p class="card-text lead">@Model.Aciklama</p>

                    <p class="text-secondary small mt-1 mb-4 italic">
                        <i class="bi bi-info-circle"></i> 
                        <em>Kutu, kurdele ve balon renkleri müşteri isteğine göre değiştirilebilir.</em>
                    </p>

                    <div class="alert alert-info border-0 shadow-sm d-flex align-items-center mt-auto">
                        <span class="display-4 me-3">📞</span>
                        <div>
                            <h5 class="alert-heading fw-bold mb-1">Sipariş Vermek İçin</h5>
                            <p class="mb-0 small">Bu ürünü sipariş etmek için Instagram üzerinden bize yazabilir veya arayabilirsiniz.</p>
                        </div>
                    </div>

                    <div class="d-grid gap-2 d-md-flex justify-content-md-end mt-4">
                        @if (User.Identity.IsAuthenticated)
                        {
                            <a asp-action="Edit" asp-route-id="@Model.Id" class="btn btn-warning px-4 fw-bold text-dark me-md-2">Düzenle</a>
                        }
                        <a asp-action="Index" class="btn btn-outline-secondary px-4 fw-bold">← Listeye Dön</a>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>